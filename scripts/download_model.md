# Local model files

The original repository contains pretrained AASIST weights under `models/weights/`. They are intentionally ignored by Git so the repository stays lightweight.

Copy `AASIST.pth` and optionally `AASIST-L.pth` from your original local project into:

```text
models/weights/
```

For a clean clone, place the licensed model files there before running inference.
